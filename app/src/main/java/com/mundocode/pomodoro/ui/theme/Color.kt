package com.mundocode.pomodoro.ui.theme

import androidx.compose.material3.darkColorScheme
import androidx.compose.material3.lightColorScheme
import androidx.compose.ui.graphics.Color

// Modo Claro
val LightColorScheme = lightColorScheme(
    primary = Color(0xFFB51C1C), // Fondo
    onPrimary = Color(0xFFFFFFFF), // Texto
    secondary = Color(0xFF06B6D4), // Fondo
    onSecondary = Color(0xFFFFFFFF), // Texto
    tertiary = Color(0xFF6366F1), // Fondo
    onTertiary = Color(0xFFFFFFFF), // Texto
    background = Color(0xFFEFEFEF), // Fondo
    surface = Color(0xFFFFFFFF), // Fondo
    inverseSurface = Color(0xFF000000),
    onSurface = Color(0xFF000000), // Texto
    onSurfaceVariant = Color(0xFF000000), // Texto
)

// Modo Oscuro
val DarkColorScheme = darkColorScheme(
    primary = Color(0xFF6366F1), // Fondo
    onPrimary = Color(0xFFFFFFFF), // Texto
    secondary = Color(0xFF6366F1), // Fondo
    onSecondary = Color(0xFFFFFFFF), // Texto
    tertiary = Color(0xFF6366F1), // Fondo
    onTertiary = Color(0xFFFFFFFF), // Texto
    background = Color(0xFF192229), // Fondo
    surface = Color(0xFF6366F1), // Fondo
    inverseSurface = Color(0xFFFFFFFF),
    onSurface = Color(0xFFFFFFFF), // Texto
    onSurfaceVariant = Color(0xFF000000), // Texto
)

// // Modo Azul
// val BlueColorScheme = lightColorScheme(
//    primary = Color(), // Fondo
//    onPrimary = Color(), // Texto
//    secondary = Color(), // Fondo
//    onSecondary = Color(), // Texto
//    tertiary = Color(), // Fondo
//    onTertiary = Color(), // Texto
//    background = Color(0xFF2196F3), // Fondo
//    surface = Color(), // Fondo
//    onSurface = Color(), // Texto
// )
//
// // Modo Rojo
// val RedColorScheme = lightColorScheme(
//    primary = Color(), // Fondo
//    onPrimary = Color(), // Texto
//    secondary = Color(), // Fondo
//    onSecondary = Color(), // Texto
//    tertiary = Color(), // Fondo
//    onTertiary = Color(), // Texto
//    background = Color(0xFFD2362D), // Fondo
//    surface = Color(), // Fondo
//    onSurface = Color(), // Texto
// )
